export const RESPONSIBLE_COLORS = {
  Deep: (theme) => theme.palette.primary.main,
  MTC: (theme) => theme.palette.secondary.main,
  Default: (theme) => theme.palette.grey[400],
};

export const ROW_HEIGHT = 40;
export const BAND_PADDING = 0.2;
